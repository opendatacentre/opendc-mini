---
ingress:
  enabled: true
  hosts:
    - dashboard.opendc.io
  annotations:
    kubernetes.io/tls-acme:      "true"
    kubernetes.io/ingress.class: nginx
  tls:
    - secretName: dashboard-tls
      hosts:
        - dashboard.opendc.io
        